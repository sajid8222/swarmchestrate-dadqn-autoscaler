# Per-service DA-DQN — 10 separate Deployments, one per scalable microservice.
# Each pod loads ONLY its own service's model (via MY_SERVICE env + sed-patch on SCALABLE_SERVICES)
# and scales ONLY its own deployment. True decentralized topology: no single point of failure.
#
# CP is tainted NoSchedule → all 10 schedule across the 4 worker nodes.
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: dadqn-frontend
  namespace: default
  labels: {app: dadqn-autoscaler, svc: frontend}
spec:
  replicas: 1
  selector:
    matchLabels: {app: dadqn-autoscaler, svc: frontend}
  template:
    metadata:
      labels: {app: dadqn-autoscaler, svc: frontend, istio.io/dataplane-mode: none}
    spec:
      serviceAccountName: dadqn-autoscaler
      containers:
        - name: agent
          image: proactivellmbasedproject/dadqn-autoscaler:v4-decentralized
          imagePullPolicy: Always
          command: ["sh","-c"]
          args:
            - >
              python -c "import os,re; svc=os.environ['MY_SERVICE']; p='/app/dadqn_v3/config.py'; s=open(p).read(); s=re.sub(r'^SCALABLE_SERVICES = \[[\s\S]*?\]', f'SCALABLE_SERVICES = [\"{svc}\"]', s, flags=re.M); open(p,'w').write(s); print('agent =',svc)" &&
              find /app -name '*.pyc' -delete 2>/dev/null;
              exec python -m dadqn_v3.baselines.dadqn_serve
          env:
            - {name: MY_SERVICE, value: frontend}
          envFrom: [{configMapRef: {name: dadqn-config}}]
          volumeMounts:
            - {name: data, mountPath: /data}
          resources:
            requests: {cpu: "100m", memory: "256Mi"}
            limits: {cpu: "500m", memory: "1Gi"}
      volumes:
        - {name: data, emptyDir: {}}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: dadqn-cartservice
  namespace: default
  labels: {app: dadqn-autoscaler, svc: cartservice}
spec:
  replicas: 1
  selector:
    matchLabels: {app: dadqn-autoscaler, svc: cartservice}
  template:
    metadata:
      labels: {app: dadqn-autoscaler, svc: cartservice, istio.io/dataplane-mode: none}
    spec:
      serviceAccountName: dadqn-autoscaler
      containers:
        - name: agent
          image: proactivellmbasedproject/dadqn-autoscaler:v4-decentralized
          imagePullPolicy: Always
          command: ["sh","-c"]
          args:
            - >
              python -c "import os,re; svc=os.environ['MY_SERVICE']; p='/app/dadqn_v3/config.py'; s=open(p).read(); s=re.sub(r'^SCALABLE_SERVICES = \[[\s\S]*?\]', f'SCALABLE_SERVICES = [\"{svc}\"]', s, flags=re.M); open(p,'w').write(s); print('agent =',svc)" &&
              find /app -name '*.pyc' -delete 2>/dev/null;
              exec python -m dadqn_v3.baselines.dadqn_serve
          env:
            - {name: MY_SERVICE, value: cartservice}
          envFrom: [{configMapRef: {name: dadqn-config}}]
          volumeMounts:
            - {name: data, mountPath: /data}
          resources:
            requests: {cpu: "100m", memory: "256Mi"}
            limits: {cpu: "500m", memory: "1Gi"}
      volumes:
        - {name: data, emptyDir: {}}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: dadqn-productcatalogservice
  namespace: default
  labels: {app: dadqn-autoscaler, svc: productcatalogservice}
spec:
  replicas: 1
  selector:
    matchLabels: {app: dadqn-autoscaler, svc: productcatalogservice}
  template:
    metadata:
      labels: {app: dadqn-autoscaler, svc: productcatalogservice, istio.io/dataplane-mode: none}
    spec:
      serviceAccountName: dadqn-autoscaler
      containers:
        - name: agent
          image: proactivellmbasedproject/dadqn-autoscaler:v4-decentralized
          imagePullPolicy: Always
          command: ["sh","-c"]
          args:
            - >
              python -c "import os,re; svc=os.environ['MY_SERVICE']; p='/app/dadqn_v3/config.py'; s=open(p).read(); s=re.sub(r'^SCALABLE_SERVICES = \[[\s\S]*?\]', f'SCALABLE_SERVICES = [\"{svc}\"]', s, flags=re.M); open(p,'w').write(s); print('agent =',svc)" &&
              find /app -name '*.pyc' -delete 2>/dev/null;
              exec python -m dadqn_v3.baselines.dadqn_serve
          env:
            - {name: MY_SERVICE, value: productcatalogservice}
          envFrom: [{configMapRef: {name: dadqn-config}}]
          volumeMounts:
            - {name: data, mountPath: /data}
          resources:
            requests: {cpu: "100m", memory: "256Mi"}
            limits: {cpu: "500m", memory: "1Gi"}
      volumes:
        - {name: data, emptyDir: {}}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: dadqn-currencyservice
  namespace: default
  labels: {app: dadqn-autoscaler, svc: currencyservice}
spec:
  replicas: 1
  selector:
    matchLabels: {app: dadqn-autoscaler, svc: currencyservice}
  template:
    metadata:
      labels: {app: dadqn-autoscaler, svc: currencyservice, istio.io/dataplane-mode: none}
    spec:
      serviceAccountName: dadqn-autoscaler
      containers:
        - name: agent
          image: proactivellmbasedproject/dadqn-autoscaler:v4-decentralized
          imagePullPolicy: Always
          command: ["sh","-c"]
          args:
            - >
              python -c "import os,re; svc=os.environ['MY_SERVICE']; p='/app/dadqn_v3/config.py'; s=open(p).read(); s=re.sub(r'^SCALABLE_SERVICES = \[[\s\S]*?\]', f'SCALABLE_SERVICES = [\"{svc}\"]', s, flags=re.M); open(p,'w').write(s); print('agent =',svc)" &&
              find /app -name '*.pyc' -delete 2>/dev/null;
              exec python -m dadqn_v3.baselines.dadqn_serve
          env:
            - {name: MY_SERVICE, value: currencyservice}
          envFrom: [{configMapRef: {name: dadqn-config}}]
          volumeMounts:
            - {name: data, mountPath: /data}
          resources:
            requests: {cpu: "100m", memory: "256Mi"}
            limits: {cpu: "500m", memory: "1Gi"}
      volumes:
        - {name: data, emptyDir: {}}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: dadqn-recommendationservice
  namespace: default
  labels: {app: dadqn-autoscaler, svc: recommendationservice}
spec:
  replicas: 1
  selector:
    matchLabels: {app: dadqn-autoscaler, svc: recommendationservice}
  template:
    metadata:
      labels: {app: dadqn-autoscaler, svc: recommendationservice, istio.io/dataplane-mode: none}
    spec:
      serviceAccountName: dadqn-autoscaler
      containers:
        - name: agent
          image: proactivellmbasedproject/dadqn-autoscaler:v4-decentralized
          imagePullPolicy: Always
          command: ["sh","-c"]
          args:
            - >
              python -c "import os,re; svc=os.environ['MY_SERVICE']; p='/app/dadqn_v3/config.py'; s=open(p).read(); s=re.sub(r'^SCALABLE_SERVICES = \[[\s\S]*?\]', f'SCALABLE_SERVICES = [\"{svc}\"]', s, flags=re.M); open(p,'w').write(s); print('agent =',svc)" &&
              find /app -name '*.pyc' -delete 2>/dev/null;
              exec python -m dadqn_v3.baselines.dadqn_serve
          env:
            - {name: MY_SERVICE, value: recommendationservice}
          envFrom: [{configMapRef: {name: dadqn-config}}]
          volumeMounts:
            - {name: data, mountPath: /data}
          resources:
            requests: {cpu: "100m", memory: "256Mi"}
            limits: {cpu: "500m", memory: "1Gi"}
      volumes:
        - {name: data, emptyDir: {}}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: dadqn-checkoutservice
  namespace: default
  labels: {app: dadqn-autoscaler, svc: checkoutservice}
spec:
  replicas: 1
  selector:
    matchLabels: {app: dadqn-autoscaler, svc: checkoutservice}
  template:
    metadata:
      labels: {app: dadqn-autoscaler, svc: checkoutservice, istio.io/dataplane-mode: none}
    spec:
      serviceAccountName: dadqn-autoscaler
      containers:
        - name: agent
          image: proactivellmbasedproject/dadqn-autoscaler:v4-decentralized
          imagePullPolicy: Always
          command: ["sh","-c"]
          args:
            - >
              python -c "import os,re; svc=os.environ['MY_SERVICE']; p='/app/dadqn_v3/config.py'; s=open(p).read(); s=re.sub(r'^SCALABLE_SERVICES = \[[\s\S]*?\]', f'SCALABLE_SERVICES = [\"{svc}\"]', s, flags=re.M); open(p,'w').write(s); print('agent =',svc)" &&
              find /app -name '*.pyc' -delete 2>/dev/null;
              exec python -m dadqn_v3.baselines.dadqn_serve
          env:
            - {name: MY_SERVICE, value: checkoutservice}
          envFrom: [{configMapRef: {name: dadqn-config}}]
          volumeMounts:
            - {name: data, mountPath: /data}
          resources:
            requests: {cpu: "100m", memory: "256Mi"}
            limits: {cpu: "500m", memory: "1Gi"}
      volumes:
        - {name: data, emptyDir: {}}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: dadqn-adservice
  namespace: default
  labels: {app: dadqn-autoscaler, svc: adservice}
spec:
  replicas: 1
  selector:
    matchLabels: {app: dadqn-autoscaler, svc: adservice}
  template:
    metadata:
      labels: {app: dadqn-autoscaler, svc: adservice, istio.io/dataplane-mode: none}
    spec:
      serviceAccountName: dadqn-autoscaler
      containers:
        - name: agent
          image: proactivellmbasedproject/dadqn-autoscaler:v4-decentralized
          imagePullPolicy: Always
          command: ["sh","-c"]
          args:
            - >
              python -c "import os,re; svc=os.environ['MY_SERVICE']; p='/app/dadqn_v3/config.py'; s=open(p).read(); s=re.sub(r'^SCALABLE_SERVICES = \[[\s\S]*?\]', f'SCALABLE_SERVICES = [\"{svc}\"]', s, flags=re.M); open(p,'w').write(s); print('agent =',svc)" &&
              find /app -name '*.pyc' -delete 2>/dev/null;
              exec python -m dadqn_v3.baselines.dadqn_serve
          env:
            - {name: MY_SERVICE, value: adservice}
          envFrom: [{configMapRef: {name: dadqn-config}}]
          volumeMounts:
            - {name: data, mountPath: /data}
          resources:
            requests: {cpu: "100m", memory: "256Mi"}
            limits: {cpu: "500m", memory: "1Gi"}
      volumes:
        - {name: data, emptyDir: {}}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: dadqn-shippingservice
  namespace: default
  labels: {app: dadqn-autoscaler, svc: shippingservice}
spec:
  replicas: 1
  selector:
    matchLabels: {app: dadqn-autoscaler, svc: shippingservice}
  template:
    metadata:
      labels: {app: dadqn-autoscaler, svc: shippingservice, istio.io/dataplane-mode: none}
    spec:
      serviceAccountName: dadqn-autoscaler
      containers:
        - name: agent
          image: proactivellmbasedproject/dadqn-autoscaler:v4-decentralized
          imagePullPolicy: Always
          command: ["sh","-c"]
          args:
            - >
              python -c "import os,re; svc=os.environ['MY_SERVICE']; p='/app/dadqn_v3/config.py'; s=open(p).read(); s=re.sub(r'^SCALABLE_SERVICES = \[[\s\S]*?\]', f'SCALABLE_SERVICES = [\"{svc}\"]', s, flags=re.M); open(p,'w').write(s); print('agent =',svc)" &&
              find /app -name '*.pyc' -delete 2>/dev/null;
              exec python -m dadqn_v3.baselines.dadqn_serve
          env:
            - {name: MY_SERVICE, value: shippingservice}
          envFrom: [{configMapRef: {name: dadqn-config}}]
          volumeMounts:
            - {name: data, mountPath: /data}
          resources:
            requests: {cpu: "100m", memory: "256Mi"}
            limits: {cpu: "500m", memory: "1Gi"}
      volumes:
        - {name: data, emptyDir: {}}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: dadqn-paymentservice
  namespace: default
  labels: {app: dadqn-autoscaler, svc: paymentservice}
spec:
  replicas: 1
  selector:
    matchLabels: {app: dadqn-autoscaler, svc: paymentservice}
  template:
    metadata:
      labels: {app: dadqn-autoscaler, svc: paymentservice, istio.io/dataplane-mode: none}
    spec:
      serviceAccountName: dadqn-autoscaler
      containers:
        - name: agent
          image: proactivellmbasedproject/dadqn-autoscaler:v4-decentralized
          imagePullPolicy: Always
          command: ["sh","-c"]
          args:
            - >
              python -c "import os,re; svc=os.environ['MY_SERVICE']; p='/app/dadqn_v3/config.py'; s=open(p).read(); s=re.sub(r'^SCALABLE_SERVICES = \[[\s\S]*?\]', f'SCALABLE_SERVICES = [\"{svc}\"]', s, flags=re.M); open(p,'w').write(s); print('agent =',svc)" &&
              find /app -name '*.pyc' -delete 2>/dev/null;
              exec python -m dadqn_v3.baselines.dadqn_serve
          env:
            - {name: MY_SERVICE, value: paymentservice}
          envFrom: [{configMapRef: {name: dadqn-config}}]
          volumeMounts:
            - {name: data, mountPath: /data}
          resources:
            requests: {cpu: "100m", memory: "256Mi"}
            limits: {cpu: "500m", memory: "1Gi"}
      volumes:
        - {name: data, emptyDir: {}}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: dadqn-emailservice
  namespace: default
  labels: {app: dadqn-autoscaler, svc: emailservice}
spec:
  replicas: 1
  selector:
    matchLabels: {app: dadqn-autoscaler, svc: emailservice}
  template:
    metadata:
      labels: {app: dadqn-autoscaler, svc: emailservice, istio.io/dataplane-mode: none}
    spec:
      serviceAccountName: dadqn-autoscaler
      containers:
        - name: agent
          image: proactivellmbasedproject/dadqn-autoscaler:v4-decentralized
          imagePullPolicy: Always
          command: ["sh","-c"]
          args:
            - >
              python -c "import os,re; svc=os.environ['MY_SERVICE']; p='/app/dadqn_v3/config.py'; s=open(p).read(); s=re.sub(r'^SCALABLE_SERVICES = \[[\s\S]*?\]', f'SCALABLE_SERVICES = [\"{svc}\"]', s, flags=re.M); open(p,'w').write(s); print('agent =',svc)" &&
              find /app -name '*.pyc' -delete 2>/dev/null;
              exec python -m dadqn_v3.baselines.dadqn_serve
          env:
            - {name: MY_SERVICE, value: emailservice}
          envFrom: [{configMapRef: {name: dadqn-config}}]
          volumeMounts:
            - {name: data, mountPath: /data}
          resources:
            requests: {cpu: "100m", memory: "256Mi"}
            limits: {cpu: "500m", memory: "1Gi"}
      volumes:
        - {name: data, emptyDir: {}}
